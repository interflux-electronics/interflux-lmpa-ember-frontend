<div>
  <h2><strong>Get in touch</strong></h2>
  <h3><a href="mailto:lmpa@interflux.com" target="_blank" rel="noopener">lmpa@interflux.com</a></h3>

  <button onclick={{perform fetchCountries}}>
    {{if fetchCountries.isRunning 'fetching' 'Fetch Countries'}}
  </button>
  {{#if countries}}
    <ul>
      {{#each countries as |country|}}
        <li>{{country.name}} ({{country.nativeName}})</li>
      {{/each}}
    </ul>
  {{/if}}

</div>
